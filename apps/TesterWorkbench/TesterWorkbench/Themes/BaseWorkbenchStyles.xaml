<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.AppBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type ToolBar}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
    </Style>

    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="Padding" Value="8,4" />
    </Style>

    <Style TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.Border}" />
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="Padding" Value="10,3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="ButtonChrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundHover}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.BorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundPressed}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextMuted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="Padding" Value="10,3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="ToggleChrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundHover}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.BorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundPressed}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundPressed}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextMuted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="Padding" Value="8,3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="22" />
                        </Grid.ColumnDefinitions>
                        <ToggleButton x:Name="ComboToggle"
                                      Grid.ColumnSpan="2"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      Foreground="{TemplateBinding Foreground}"
                                      Focusable="False"
                                      Padding="0"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Stretch"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="22" />
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="1"
                                      Width="8"
                                      Height="5"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Fill="{DynamicResource Workbench.Brush.TextSecondary}"
                                      Data="M 0 0 L 8 0 L 4 5 Z" />
                            </Grid>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                          Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          IsHitTestVisible="False"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                        <TextBox x:Name="PART_EditableTextBox"
                                 Grid.Column="0"
                                 Margin="{TemplateBinding Padding}"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Stretch"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{TemplateBinding Foreground}"
                                 CaretBrush="{TemplateBinding Foreground}"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"
                                 Visibility="Hidden" />
                        <Popup x:Name="Popup"
                               Grid.ColumnSpan="2"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               PopupAnimation="Fade">
                            <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{DynamicResource Workbench.Brush.ControlBackground}"
                                    BorderBrush="{DynamicResource Workbench.Brush.BorderStrong}"
                                    BorderThickness="1">
                                <ScrollViewer CanContentScroll="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ComboToggle" Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundHover}" />
                            <Setter TargetName="ComboToggle" Property="BorderBrush" Value="{DynamicResource Workbench.Brush.BorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="ComboToggle" Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundPressed}" />
                            <Setter TargetName="ComboToggle" Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextMuted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static ToolBar.ComboBoxStyleKey}"
           TargetType="{x:Type ComboBox}"
           BasedOn="{StaticResource {x:Type ComboBox}}" />

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder"
                                    Property="Background"
                                    Value="{DynamicResource Workbench.Brush.ControlBackgroundHover}" />
                            <Setter TargetName="ItemBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Workbench.Brush.BorderStrong}" />
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Workbench.Brush.TextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder"
                                    Property="Background"
                                    Value="{DynamicResource Workbench.Brush.ControlBackgroundPressed}" />
                            <Setter TargetName="ItemBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Workbench.Brush.Accent}" />
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Workbench.Brush.TextPrimary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="CaretBrush" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type GroupBox}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Grid.RowSpan="2"
                                Margin="0,9,0,0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="2" />
                        <Border Grid.Row="0"
                                Margin="8,0,0,0"
                                Padding="6,0"
                                Background="{DynamicResource Workbench.Brush.AppBackground}"
                                HorizontalAlignment="Left">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{DynamicResource Workbench.Brush.TextSecondary}" />
                        </Border>
                        <ContentPresenter Grid.Row="1"
                                          Margin="8,12,8,8" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TreeView}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type TreeViewItem}">
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type TabItem}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackgroundAlt}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextSecondary}" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Margin" Value="0,0,2,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="TabChrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3,3,0,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.ControlBackgroundHover}" />
                            <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.BorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Panel.ZIndex" Value="1" />
                            <Setter Property="Background" Value="{DynamicResource Workbench.Brush.TabSelectedBackground}" />
                            <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TabSelectedText}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Accent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextMuted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="RowBackground" Value="{DynamicResource Workbench.Brush.PanelBackground}" />
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource Workbench.Brush.PanelBackgroundAlt}" />
    </Style>

    <Style TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.PanelBackgroundAlt}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextSecondary}" />
    </Style>

    <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource Workbench.Brush.Border}" />
        <Setter Property="Foreground" Value="{DynamicResource Workbench.Brush.TextPrimary}" />
    </Style>

    <Style TargetType="{x:Type GridSplitter}">
        <Setter Property="Background" Value="{DynamicResource Workbench.Brush.AppBackground}" />
    </Style>
</ResourceDictionary>
