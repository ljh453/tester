functions:
  power_on:
    params: [channel]
    returns: [voltage_ok]
    steps:
      - power_supply.command:
          device: psu
          action: output
          channel: "${channel}"
          state: on
      - delay:
          ms: 300
      - set:
          var: voltage_ok
          value: true
